import { Card } from 'antd';
import { MyButton } from './MyButton';

export const MyButtonWrapper = () => {
  return (
    <Card title="MyButton">
      <MyButton>click</MyButton>
    </Card>
  );
};
